# Common configuration shared between all applications
configserver:
  name: Docker JHipster Registry
  status: Connected to the JHipster Registry running in Docker

jhipster:
  security:
    authentication:
      jwt:
        secret: srthertherthrtherth

eureka:
  client:
    service-url:
      defaultZone: http://soldimet:${jhipster.registry.password}@localhost:8761/eureka/
